<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.renatoandrade.desafiohorizon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SurfistaAlteracaoActivity"></activity>
        <activity android:name=".BateriaAdicionarActivity" />
        <activity android:name=".BateriaBuscarActivity" />
        <activity android:name=".SurfistaResultadoBuscaActivity" />
        <activity android:name=".SurfistaBuscarActivity" />
        <activity android:name=".SurfistaAdicionarActivity" />
        <activity android:name=".BateriaActivity" />
        <activity android:name=".SurfistaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>